{
  "name": "urlopy",
  "version": "1.0.0",
  "description": "## Set-up",
  "main": "index.js",
  "scripts": {
    "install": "cd client && yarn && cd .. && cd server && yarn",
    "start:client": "cd client && yarn && yarn start",
    "start:server": "cd server && yarn && yarn start",
    "start:server:watch": "cd server && yarn && yarn start:watch",
    "test:server": "./node_modules/mocha/bin/mocha common server",
    "test:client": "cd client && yarn test",
    "test": "yarn run test:server && echo Press Enter to run client tests... && read && yarn run test:client"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/syzygypl/urlopy-app.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/syzygypl/urlopy-app/issues"
  },
  "homepage": "https://github.com/syzygypl/urlopy-app#readme",
  "devDependencies": {
    "mocha": "^3.2.0"
  }
}
